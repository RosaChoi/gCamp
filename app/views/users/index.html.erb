<div class="page-header">
  <div class="pull-right">
    <%= link_to "New Users", new_user_path, class: "btn btn-info" %>
  </div>
  <h1>Users</h1>
</div>

<table class="table table-responsive">
  <thead>
    <tr>
      <th class="col-sm-3">Name</th>
      <th class="col-sm-9">Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to full_name = "#{user.first_name} #{user.last_name}", user %></td>
        <td><%= mail_to user.email %></td>
        <td><%= link_to 'Edit', edit_user_path(user) %></td>
    <% end %>
  </tbody>
</table>
